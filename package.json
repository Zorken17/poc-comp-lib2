{
  "name": "@ab.poc/root",
  "private": true,
  "workspaces": [
    "packages/**/*",
    "playgrounds/*"
  ],
  "devDependencies": {
    "lerna": "^8.0.0"
  },
  "scripts": {
    "build": "yarn lerna run build",
    "dev": "yarn lerna run dev",
    "publish": "yarn build && yarn lerna publish"
  }
}
